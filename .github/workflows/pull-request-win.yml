# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_pull-request-win --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pull-request-win

on:
  pull_request:
    branches:
      - main

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Rebuild, CodeCoverage, CreateWin64Setup'
        run: ./build.cmd Rebuild CodeCoverage CreateWin64Setup
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: coverage_report'
        uses: actions/upload-artifact@v3
        with:
          name: coverage_report
          path: .tests/coverage_report
      - name: 'Publish: setups'
        uses: actions/upload-artifact@v3
        with:
          name: setups
          path: .artifacts/setups
